import { DefaultLayout } from '@/components'
import { Settings } from '@/modules'

const SettingsPage = () => {
  return (
    <DefaultLayout>
      <Settings />
    </DefaultLayout>
  )
}

export default SettingsPage
